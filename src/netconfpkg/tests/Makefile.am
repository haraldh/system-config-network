TESTS_ENVIRONMENT = PYTHONPATH=${srcdir}/../:@PY_PYTHONPATH@
TESTS = test-Conf.py test-rcn.py
EXTRA_DIST = $(TESTS) basic.tar coverage.py

CLEANFILES := .coverage stdout stderr $(notdir $(wildcard *,cover)) $(notdir $(wildcard *~)) $(notdir $(wildcard *\#))


